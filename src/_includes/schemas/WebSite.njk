<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "WebSite",
        "@id": "{{ meta.url }}#website",
        "url": "{{ meta.url }}",
        "name": "{{ meta.siteName | escapeJson | safe }}",
        "description": "{{ meta.siteDescription | escapeJson | safe }}",
        "inLanguage": "{{ meta.locale }}"
      }
    ]
  }
</script>
